<div class="search">
  <%= form_for(books_path, method: :get) do |f| %>
    <%= text_field_tag :search, params[:search], class:"search_box", placeholder: 'Search Books'%>
    <button type="submit" id="search_button">
      <span class="glyphicon glyphicon-search "/>
    </button>
  <% end %>
</div>
<br/><br/>

  <%= link_to 'View All', :books_list, id: 'link'%>

<%= render :partial => 'layouts/book_list' %>
